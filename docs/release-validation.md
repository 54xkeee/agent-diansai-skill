# 2026-09-06 人机协作入口：验证记录

## 第二轮：从历史共性重写协作机制

初版以下记录保持为历史，不把初版抽样扩写成全量阅读。本轮维护源仍为本仓库，修改前Git基线为`1d6cdaa90de41e0b7f69186d3f35cde61394457a`，可恢复到初版；未修改用户配置或插件缓存。

### 范围与证据

- 重写协作入口与两份参考：共同事实的关系、纠正后的依赖更新、重复失败时改变取证、操作接力、工况迁移、方案级归因与长对话交接。
- 新增历史审计与情境回放，更新首页、仓库入口说明和经验帖子；初版三个项目复盘注明其抽样范围。
- 本地历史索引30个主任务、44个子任务；全文阅读去重用户文本，助手决策摘要导航加关键段落完整回读，event记录作补充。加密正文、图像与工具输出的覆盖限制见[审计](diansai-dialogue-audit.md)。不声称所有历史材料逐字验收。
- 本地证据台账保留9条链、142个引用条目的源日志行号；原始历史及台账未纳入Git发布。
- 旧专业skill实现、安装器、安装测试、UI元数据未改变；原始电赛固件及另外两个项目未作修改或烧录。

### 已执行的本轮检查

1. 新版`diansai-collab`的`quick_validate.py`通过，使用`PYTHONUTF8=1`。
2. Windows隔离安装回归重新运行：**9/9通过**，覆盖新参考文件的复制与内容校验，不更新真实用户技能目录。
3. 新增/修改正文的UTF-8、本地链接与隐私高风险模式检查通过，并人工审阅公开的案例概括；发布清单不含原始聊天和私人附件。
4. 覆盖清单30行、证据链9节、情境推演18项数量核对一致；关键历史段落与台账相互对应，重复/继承记录未作为独立实验计数。
5. 对18个情境作具体决策推演及过度约束反查。**这是编写代理的事后自审，不是18/18模型测试通过，也不是独立盲测或新生效果验证。**
6. 安装器、测试、旧完整入口、UI元数据与初版Git内容核对未发生语义变更；`git diff --check`通过。

### 仍未验证

尚无独立新会话行为A/B、真实新生学习或完成率数据；尚未安装至当前用户目录或证明自动加载。没有重做历史实机、没有在小红书代发。GitHub交付以推送后远端提交与文件内容核对为准，不用本地检查代替发布结果。

---

## 第一轮记录

## 变更范围

- 新增 `diansai-collab`：短反馈回路、分工、纠错、现场反馈、轻量交接及两份参考材料。
- 首页和仓库规则转向新手人机协作；旧 `nuedc-full-runner` 收窄为明确选择时的专业流程，移除当前格式检查器不支持的旧 `user-invocable` 字段。
- 安装器默认单入口，保留按名称选择与全量安装；覆盖前备份、拒绝链接目标、逐源文件 SHA-256 校验。
- 新增三个项目复盘、小红书文案和安装回归测试。
- 维护源为本仓库，修改前 Git 基线为 `34dc014baaa7403c85d95ef61718ef0f6ddc0f1a`。原固件工程、生成示例和其他技能实现保持原样；用户安装目录未在本轮更新。

## 已执行的检查

1. 新 skill 的 `quick_validate.py`：通过。
2. 已修改旧入口的相同格式检查：通过。
3. Windows 隔离目录安装回归：**9/9**。默认只装 1 个、指定旧技能、全量 7 个、已有同名备份、其他技能保留、非法路径和缺失源在写入前退出、源目标相同退出、环境变量目标、Junction 目标退出。每个成功安装用源文件与目标文件的 SHA-256 比较，并核对技能数量。
4. 本次新增/修改 Markdown 的本地链接、UTF-8、元数据，以及变更文件的秘密信息高风险模式检查；结合人工复核，未将原始对话或凭据纳入发布清单。
5. `git diff --check`：通过。仅提交列出的 skill、入口、安装器、文档和测试文件。

安装测试命令：`python tests/test_install.py`，需要 Windows PowerShell。测试在新建临时目录执行，保留现场便于检查，不操作实际用户技能目录，也不连接硬件。

### 过程中保留的失败

- 首次中文 skill 校验遇系统 GBK 默认解码错误；设置 `PYTHONUTF8=1` 后通过，未改写校验器。
- 首轮安装回归为 4/9 通过，5 项安装场景因子进程找不到 `Get-FileHash` 失败。改用 .NET SHA-256 后重新跑完整集合，9/9 通过。
- 旧入口检查发现不支持的 `user-invocable` 字段，删除该旧字段并复测。

这些修正是安装与格式层面的结果，不是实际电赛项目验收。

## 协作场景自审

这是作者代理逐项检查设计的桌面推演，**不是独立模型测试、真实用户试验或效率评测**。

| 输入情境 | 应采取的行为 | 本次检查 |
|---|---|---|
| 大一新生只说“我想做这道题” | 解释输入/输出和最小目标，不先派完整课程 | 新入口提供入门方式，专业长流程不作为默认 |
| “ticks 为零，帮我调 PI” | 先核对采集链，区分观察与归因 | 入口原则与现场案例均覆盖 |
| “线窄且坏了一路” | 修正原先触发假设，不固化某个坏路索引 | 案例要求核对安装方向和真实触发 |
| “电机能转，角度太大” | 先查物理换算，再调控制 | 明确 STEP/count/位移/角度与真实力臂 |
| 用户排除串口或电压测量 | 选择等效证据，或暂停依赖的判断 | 入口写明证据边界与独立工作继续 |
| H 题要求录像，但云台经验取消视频 | 保留题目要求，调整资源或链路 | 案例明确禁止跨题照搬取舍 |
| 用户只说“上传” | 区分烧录与 Git 推送后再动作 | 执行动作的歧义边界已覆盖 |
| 编译通过，要求写成实机全部达标 | 仅报告构建层，保留实测未知 | 入口和复盘都区分验证层级 |
| 上下文变长，换聊天继续 | 优先更新已有交接，不默认生成多份流程文件 | 可选最小记录保留事实、失败和下一步 |

## 尚未验证 / 本轮未执行

- 新用户使用后完成率、耗时和获奖概率，没有对照数据。
- 新会话是否实际自动发现或加载该 skill，需在所用助手中另行确认。
- 未将新 skill 安装到作者当前用户目录；仅测试隔离安装。
- 未重跑旧专业工具的全部固件构建、烧录或实机测试；未验证其他操作系统。
- 安装器 `-FromGitHub` 在线便利分支未做端到端安装；本次实际使用克隆后本地安装路径。
- 小红书只交付文案，未向平台发帖；配图给出真实素材安排，未制造获奖证书或实物照片。
- 发布完成与否以最终交付时的 GitHub 远端 SHA 和文件内容核对为准，本文件不提前替远端状态作保证。
